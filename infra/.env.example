# =============================================================================
# PROMPT ENGINEERING STUDIO - ENVIRONMENT VARIABLES
# =============================================================================
# 
# ⚠️  SECURITY WARNING:
# - NEVER commit actual API keys to git
# - For Lovable.dev deployment, configure these in Lovable's environment settings
# - Keep this .env.example file for documentation only
#
# =============================================================================

# ---- REQUIRED: LLM API KEYS (Sensitive - Configure in Lovable.dev) ----
OPENAI_API_KEY=sk-your-openai-key-here
GOOGLE_API_KEY=AIza-your-google-ai-key-here
# Note: At least one LLM provider key is required

# ---- REQUIRED: Security Keys (Change these!) ----
LITELLM_MASTER_KEY=sk-your-secure-master-key-here
API_BEARER_TOKEN=your-secure-bearer-token-here

# ---- LiteLLM Configuration ----
LITELLM_BASE_URL=http://localhost:8001
LITELLM_SPEND_BUDGET_USD=10

# ---- Langfuse Observability (Optional but Recommended) ----
LANGFUSE_HOST=http://localhost:3002
LANGFUSE_PUBLIC_KEY=pk-lf-your-public-key
LANGFUSE_SECRET_KEY=sk-lf-your-secret-key

# ---- Database URLs ----
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/prompt_studio
LITELLM_DATABASE_URL=postgresql://postgres:postgres@localhost:5433/litellm

# ---- Flowise Workflow Engine ----
FLOWISE_API_URL=http://flowise:3000
FLOWISE_API_KEY=your-flowise-api-key
NEXT_PUBLIC_FLOWISE_URL=http://localhost:3100

# ---- Ollama (Local LLMs) ----
OLLAMA_BASE_URL=http://ollama:11434

# ---- Caching ----
REDIS_URL=redis://localhost:6379
CACHE_KEY_PREFIX=ps

# ---- SAML Authentication (Enterprise) ----
SAML_ENABLED=false
SAML_ENTRY_POINT=https://your-idp.com/saml/sso
SAML_ISSUER=your-app-identifier
SAML_CERT=your-x509-certificate

# ---- Application Configuration ----
NODE_ENV=development
PORT=4000
FRONTEND_URL=http://localhost:3000
BROKER_API_URL=http://localhost:4000

# =============================================================================
# LOVABLE.DEV DEPLOYMENT INSTRUCTIONS:
# =============================================================================
# 
# 1. In your Lovable.dev project settings, go to "Environment Variables"
# 2. Add each REQUIRED variable above with your actual values
# 3. NEVER paste actual API keys in this file or commit them to git
# 4. Test your deployment with these environment variables configured
#
# Required for Lovable.dev:
# - OPENAI_API_KEY or GOOGLE_API_KEY (at least one)
# - LITELLM_MASTER_KEY
# - API_BEARER_TOKEN
# 
# =============================================================================